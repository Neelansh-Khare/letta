# LongMemEvalS for Letta

This is the `LongMemEvalS` alignment layer for the existing cleaned LongMemEval S dataset path already used in the repository.

Current implementation notes:

- Uses `longmemeval_s_cleaned.json`
- Provides a dedicated runner name and output path for the `S` variant
- Reuses the current Letta benchmark harness while we continue broader metric expansion
